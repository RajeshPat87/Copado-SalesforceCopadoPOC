public Class CreatingAccountNew { /* To create account */
    public Account CreatingAccountNew(String name) { /* method to create account */
        Account acc = new Account();
        acc.Name = name;
        return acc;
    }
}